import wollok.game.*
import comida.*
import homero.*
import niveles.*
import tableros.*

program homeroGame{

	if(nivel1.estado() == "inicio"){
		nivel1.iniciarFondo()
		keyboard.enter().onPressDo({nivel1.iniciarNivel()})
	}
	if(nivel2.estado() == "inicio"){
		console.println("hasta aca llegue")
		nivel2.inicializarValores()
		nivel2.iniciarFondo()
		nivel2.iniciarNivel()
		keyboard.enter().onPressDo({nivel2.iniciarNivel()})
	}
}